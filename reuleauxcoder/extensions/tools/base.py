"""Compatibility imports for extension authors; contracts belong to domain."""

from reuleauxcoder.domain.tools import (
    BackendHandler as BackendHandler,
    InterruptMode as InterruptMode,
    Tool as Tool,
    ToolResult as ToolResult,
    backend_handler as backend_handler,
)
